import { Routes } from '@angular/router';
import { ProductReviewComponentComponent } from './product-review.component/product-review.component.component';

export const routes: Routes = [
    {path:'', component: ProductReviewComponentComponent}
];
